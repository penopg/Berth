// Встроенные скиллы берта: текст SKILL.md, который при первом запуске
// раскладывается в каталог ~/.config/berth/skills/<имя>/. Здесь, а не
// файлом рядом с бинарём, потому что бинарь ходит и без папки assets.
#include "skills_builtin.h"

const BuiltinSkill SKILLS_BUILTIN[] = {
{ "berth-tasks",
"---\n"
"name: berth-tasks\n"
"description: \"Задачи проекта в берте — файл .berth/tasks.md. Use when the user asks завести/добавить/записать задачи, составить список задач или план работ, поправить или переформулировать задачу, отметить сделанное, взять задачу в работу, или когда просят «запиши это как задачу». Задачи заводятся в файле, а не текстом в ответе.\"\n"
"---\n"
"\n"
"# Задачи проекта в берте\n"
"\n"
"Список задач проекта живёт в файле `.berth/tasks.md` в корне проекта. Его\n"
"показывает берт на странице проекта: там задачи раскрывают, переставляют,\n"
"отправляют в работу и отмечают сделанными. Файл правится и руками, и тобой.\n"
"\n"
"**Главное правило:** когда человек просит завести задачи, составить список\n"
"задач или план работ, записать что-то «как задачу» — задачи идут **в файл**,\n"
"а не списком в ответе. Список в ответе человек не увидит на странице и не\n"
"сможет отправить в работу. В ответе — коротко, что записано.\n"
"\n"
"Если файла нет — создай его (и папку `.berth/`, если её нет).\n"
"У подпроекта свой файл в своей папке: задачи подпроекта — в\n"
"`<подпроект>/.berth/tasks.md`, а не у родителя.\n"
"\n"
"## Формат\n"
"\n"
"Формат жёсткий — его разбирает берт:\n"
"\n"
"    Текст до первой задачи — заметки человека, сохраняй как есть.\n"
"\n"
"    ## Название задачи\n"
"    Описание в свободной форме. Абзацы через пустую строку; одиночный\n"
"    перевод строки склеивается в абзац, как в Markdown.\n"
"\n"
"    ## Вторая задача без описания\n"
"\n"
"    ## [?] Сделанная, но человек ещё не проверил\n"
"\n"
"    ## [x] Сделанная и проверенная\n"
"\n"
"- Заголовок `## ` — одна задача. Пометки состояния: без пометки — открыта,\n"
"  `[?]` — сделана, ждёт проверки, `[x]` — сделана.\n"
"- Порядок задач в файле — порядок работы: первая — самая важная. Новые\n"
"  открытые задачи ставь **после последней открытой и перед сделанными**,\n"
"  если человек не сказал, что это срочно.\n"
"- Название — одна строка, до 300 байт. Описание — до 8 КБ; это ТЗ, а не\n"
"  заметка: причина, что сделать, какие файлы, чем проверить.\n"
"- Внутри описания не используй строки, начинающиеся с `## ` — берт примет\n"
"  их за новую задачу. Заголовки внутри описания — `###` или жирным.\n"
"- Всё остальное в файле не трогай: чужие задачи, их порядок и текст\n"
"  человека до первой задачи.\n"
"\n"
"## Архив сделанных\n"
"\n"
"Задачу, помеченную `[x]`, берт при следующей записи переносит в\n"
"`.berth/tasks-done.md` — тот же формат, дописывается в конец. Отсюда:\n"
"\n"
"- сделанных задач в `tasks.md` может не быть вовсе, и это нормально;\n"
"- ищешь закрытую работу — смотри в архиве, а не решай, что её не было;\n"
"- вернуть задачу в работу — перенести её блок обратно в `tasks.md` и\n"
"  снять `[x]`. Архив берт только дописывает, чинить его можно руками.\n"
"\n"
"## Что делать по просьбе\n"
"\n"
"- **Завести задачи / составить план** — разбей работу на задачи размером в\n"
"  один осмысленный кусок (час-два работы), у каждой название и описание,\n"
"  допиши в файл. В ответе перечисли названия одной строкой каждое.\n"
"- **Поправить задачу** — найди её по заголовку дословно и перепиши текст;\n"
"  пометку состояния сохрани.\n"
"- **Взять задачу в работу** — просто делай её; когда закончишь, замени\n"
"  `## Название` на `## [?] Название`. Сразу `[x]` не ставь: проверка —\n"
"  дело человека, если он не просил иначе.\n"
"- **Отметить сделанное** по просьбе человека — `[x]`.\n"
"- **Задача пришла от берта** — в её тексте после `---` стоит наказ, какую\n"
"  пометку поставить по завершении; следуй ему.\n"
"\n"
"Проверь после правки: файл начинается как раньше, каждая задача — один\n"
"заголовок `## `, число задач сходится с тем, что ты собирался записать.\n"
},
{ "berth-data",
"---\n"
"name: berth-data\n"
"description: \"Данные проекта в берте — таблицы .berth/data/<имя>.tsv и срезы .berth/views.tsv. Use when the user asks собрать, дополнить, сравнить или отсортировать список однотипных вещей (фильмы, места, кандидаты, прогоны, варианты) с атрибутами, к которому он вернётся; добавить колонку, поправить записи, сделать срез («покажи только…»). Таблица идёт в файл, а не текстом в ответ.\"\n"
"---\n"
"\n"
"# Данные проекта в берте\n"
"\n"
"Однотипные записи с атрибутами — список фильмов с рейтингами, места и\n"
"события, кандидаты, прогоны экспериментов — живут в проекте таблицами:\n"
"`.berth/data/<имя>.tsv`. Берт показывает их на странице проекта и умеет\n"
"делать срезы; файл читается глазами и разбирается `awk`.\n"
"\n"
"Это не задачи. Задачи (`.berth/tasks.md`) — про порядок работы и\n"
"состояние «сделано». Каталог — про записи с колонками, которые\n"
"сортируют и фильтруют. Не утрамбовывай записи в `tasks.md`.\n"
"\n"
"## Когда таблица нужна, а когда нет\n"
"\n"
"Это правило важнее остальных: не превращай в таблицу любой ответ.\n"
"\n"
"Таблица нужна, когда **все три** условия выполнены:\n"
"- записей три и больше, и они однотипные — у каждой те же атрибуты;\n"
"- у записей есть атрибуты, по которым их захотят сравнить или отобрать\n"
"  (год, рейтинг, цена, статус, дата);\n"
"- человек к списку вернётся: дополнит, отсортирует, спросит про срез.\n"
"\n"
"Одноразовый вопрос («какой из этих трёх лучше?») — просто ответ.\n"
"Список без атрибутов («что взять в поход») — обычный список в ответе или\n"
"в задаче. Сомневаешься — спроси одной строкой, вести ли таблицу.\n"
"\n"
"**Главное правило:** если человек просит собрать, дополнить, сравнить или\n"
"отсортировать такой список — результат идёт **в файл-таблицу**, а в\n"
"ответе только короткая сводка: сколько записей, что добавлено, что\n"
"бросилось в глаза. Таблицу в ответ не дублируй: она умрёт вместе с\n"
"сообщением, а файл останется.\n"
"\n"
"## Формат таблицы\n"
"\n"
"`.berth/data/<имя>.tsv` — TSV: колонки через табуляцию, первая строка —\n"
"заголовки. Имя файла — короткое, латиницей, по сути: `films.tsv`,\n"
"`places.tsv`, `runs.tsv`. Папку `.berth/data/` создай, если её нет.\n"
"\n"
"    название	год	тип	жанр	режиссёр	IMDb	проверено	статус\n"
"    Улика	1985	фильм	комедия	Джонатан Линн	7.3	2026-09-06	к просмотру\n"
"\n"
"- Не CSV: запятые внутри названий ломают разбор. Не Markdown-таблица:\n"
"  она разъезжается при каждой правке. Внутри значения не должно быть\n"
"  табуляций и переводов строки — замени пробелом.\n"
"- Кодировка UTF-8 **с BOM** (три байта `EF BB BF` в начале файла):\n"
"  без него Excel открывает кириллицу кашей. `awk`, `sort` и берт BOM не\n"
"  мешает — он сидит в начале первой строки заголовков. Создавая файл,\n"
"  начни его с BOM: `printf '\\xEF\\xBB\\xBF' > файл`, затем строки.\n"
"  Проверить: `head -c 3 файл | xxd` даёт `efbb bf`.\n"
"- Excel — только смотреть: при сохранении он перекодирует файл в старую\n"
"  кодировку и ломает UTF-8. Правки — в текстовом редакторе или через\n"
"  агента.\n"
"- Заголовки — короткие, по-русски, в нижнем регистре (кроме имён\n"
"  собственных вроде IMDb). Первые колонки — те, по которым запись\n"
"  узнают (название, год), дальше атрибуты, в конце служебные (проверено,\n"
"  статус, заметка).\n"
"- Числа — числами без единиц и пробелов: `7.3`, а не `7,3` и не `7.3/10`;\n"
"  `1500`, а не `1 500 ₽`. Единицу вынеси в заголовок: `цена, ₽`. Иначе\n"
"  сортировка по колонке будет строковой.\n"
"- Даты — `ГГГГ-ММ-ДД`: сортируются как строки правильно.\n"
"- Неизвестное значение оставь **пустым**, а не выдумывай и не пиши «?».\n"
"- Значения, добытые из сети (рейтинги, цены, даты событий, наличие),\n"
"  сопровождай колонкой `проверено` с датой проверки: через полгода надо\n"
"  понимать, насколько цифра свежая. Обновил значение — обнови дату.\n"
"\n"
"## Правила правки\n"
"\n"
"- Добавил колонку — допиши её **во все строки**, хотя бы пустой.\n"
"- Порядок строк не меняй без просьбы: человек мог расставить их сам.\n"
"  Новые записи — в конец, если не сказано иначе.\n"
"- Не удаляй записи по своему усмотрению: помечай статусом\n"
"  (`отклонено`, `посмотрел`), удаляет человек.\n"
"- После правки проверь, что число табуляций одинаково во всех строках:\n"
"\n"
"      awk -F'\\t' 'NR==1{n=NF;next} NF!=n{print FILENAME\": строка \"NR\": \"NF\" колонок, надо \"n}' .berth/data/<имя>.tsv\n"
"\n"
"  Пустой вывод — файл цел.\n"
"\n"
"## Срезы: `.berth/views.tsv`\n"
"\n"
"Срез — команда-конвейер, чей вывод и есть таблица (тот же TSV с той же\n"
"строкой заголовков). Список срезов — `.berth/views.tsv`, по строке на\n"
"срез: имя, табуляция, команда. Строки с `#` в начале — комментарии.\n"
"\n"
"    # имя	команда (запускается из корня проекта через sh -c)\n"
"    Фильмы	awk -F'\\t' 'NR==1||$3==\"фильм\"' .berth/data/films.tsv\n"
"    От 7.5	awk -F'\\t' 'NR==1||$6>=7.5' .berth/data/films.tsv\n"
"    По рейтингу	awk 'NR==1' .berth/data/films.tsv; awk 'NR>1' .berth/data/films.tsv | sort -t$'\\t' -k6,6nr\n"
"\n"
"- Команда обязана **сохранять строку заголовков** — `NR==1||…` в `awk`,\n"
"  либо печатать её отдельно, как в примере с сортировкой.\n"
"- Команда запускается из корня проекта, путь к файлу — относительный.\n"
"  Внешние утилиты — только те, что есть везде: `awk`, `sort`, `grep`,\n"
"  `head`. Никаких сетевых запросов и записи файлов: срез читает, а не\n"
"  меняет.\n"
"- Срез должен отрабатывать за доли секунды: берт ждёт его не дольше двух\n"
"  секунд.\n"
"- Срезы заводятся **по просьбе** человека: «хочу видеть только короткие»,\n"
"  «покажи сериалы отдельно» — допиши строку в `views.tsv` и в ответе\n"
"  скажи, как она называется. Одноразовый вопрос про срез — ответь\n"
"  командой в терминале или сводкой, файл не трогай.\n"
"\n"
"## Что ждёт человека: фильтр таблицы\n"
"\n"
"У таблицы, которая наполняется сама (письма, выгрузки, заявки), есть\n"
"колонка-решение: `статус`, `вердикт`, `оценка`. Строка `filter` в\n"
"`.berth/shown.tsv` говорит берту, какие записи ещё ждут человека:\n"
"\n"
"    filter<TAB>.berth/data/inbox.tsv<TAB>статус<TAB>пусто<TAB>не разобрано\n"
"\n"
"Поля: путь таблицы, колонка, `пусто` или `не пусто`, подпись. Страница\n"
"показывает только такие записи (остальные складываются под «Остальные ·\n"
"N»), в заголовке «inbox · 3 не разобрано · 52 записи», в панели у имени\n"
"проекта «· 3 не разобрано» цветом внимания. Ноль — панель молчит. Один\n"
"фильтр на таблицу; заводи его, когда у таблицы есть колонка, которую\n"
"человек заполняет по мере разбора, — иначе всё будет «ждать» вечно.\n"
"- Имя среза — короткое, по-русски, как подпись кнопки: «Сериалы»,\n"
"  «Дешевле 2000», «Свежие».\n"
"\n"
"## Что делать по просьбе\n"
"\n"
"- **Собрать список** — заведи `.berth/data/<имя>.tsv` с заголовками и\n"
"  записями; в ответе — сколько записей и какие колонки. Для значений из\n"
"  сети — колонка `проверено` с сегодняшней датой. Тут же добавь таблицу\n"
"  в реестр документов `.berth/files.tsv` (путь, табуляция, что это одной\n"
"  фразой — см. скилл `berth-files`): так она появится на странице проекта.\n"
"- **Кнопка над таблицей** («хочу отмечать одним кликом») — это скилл\n"
"  `berth-actions`: действия живут в `.berth/actions.tsv`.\n"
"- **Дополнить** — допиши строки в конец, все колонки; новых колонок без\n"
"  нужды не заводи.\n"
"- **Сравнить или отсортировать** — не переписывай файл ради порядка:\n"
"  ответь сводкой, а если человек хочет видеть это постоянно — срезом в\n"
"  `views.tsv`.\n"
"- **Обновить рейтинги/цены** — перепиши значения и дату в `проверено`;\n"
"  в ответе перечисли, что изменилось.\n"
"- **Перенести список из tasks.md или из текста** — заведи таблицу, а в\n"
"  исходном месте оставь ссылку на файл; записи человека не удаляй без\n"
"  спроса.\n"
"\n"
"Проверь после правки: заголовок на месте, число колонок одинаково во всех\n"
"строках, число записей сходится с тем, что ты собирался записать.\n"
},
{ "berth-files",
"---\n"
"name: berth-files\n"
"description: \"Реестр документов проекта в берте — файл .berth/files.tsv: путь и пояснение, что это, одной фразой. Use when you create or substantially rewrite a document, table, report, draft, export, screencast or recording meant for a human (not code), or when the user asks «опиши, что лежит в проекте», «что это за файл», «разбери документы», «обнови реестр файлов». Пояснение идёт в файл, а не в ответ.\"\n"
"---\n"
"\n"
"# Реестр документов проекта\n"
"\n"
"В папке проекта копятся документы, таблицы, отчёты и черновики, и через\n"
"неделю не понять, что есть что. Реестр `.berth/files.tsv` отвечает на\n"
"вопрос «что это за файл» без открытия файла; берт показывает его на\n"
"странице проекта. Файл читается глазами и правится руками.\n"
"\n"
"## Формат\n"
"\n"
"По строке на документ: путь относительно корня проекта, табуляция,\n"
"пояснение одной фразой. Строки с `#` в начале — комментарии.\n"
"\n"
"    # путь	что это\n"
"    docs/themes.md	Проект системы тем: тема — файл в формате Ghostty, роли интерфейса выводятся из палитры; отложен до обсуждения\n"
"    ROADMAP.md	Позиционирование продукта и этапы работы; первые этапы закрыты, дальше — план\n"
"    .berth/data/films.tsv	Список фильмов и сериалов к просмотру с рейтингами IMDb и статусом\n"
"\n"
"- Пояснение — до 200 знаков, по-русски, как ответ на «что это?»: что за\n"
"  документ, о чём, в каком состоянии (черновик, актуален, отложен).\n"
"  Не пересказ содержимого и не повтор имени файла.\n"
"- Даты в реестре **нет**: она врёт после правки руками. Берт берёт время\n"
"  правки с самого файла.\n"
"- Порядок строк — порядок в файле, берт его не меняет. Новые строки — в\n"
"  конец, если человек не расставил их сам.\n"
"- Папку `.berth/` создай, если её нет. Путь — с прямыми слэшами, без\n"
"  `./` впереди.\n"
"\n"
"## Что попадает в реестр — главное правило\n"
"\n"
"Только файлы, **сделанные для человека**, а не для машины: документы,\n"
"таблицы, отчёты, черновики, экспорты, картинки, презентации — и записи:\n"
"скринкасты, видео, звук. Артефакт, который человек откроет и посмотрит,\n"
"тоже документ: через неделю «Скринкаст.mp4» сам о себе не расскажет.\n"
"\n"
"Не попадают:\n"
"- код, тесты, конфиги, сборочные скрипты, зависимости — их описывает\n"
"  структура репозитория и паспорт; реестр из каждого `.c` утонет;\n"
"- служебные файлы берта: `.berth/tasks.md`, `journal.md`, `summary.md`,\n"
"  `views.tsv`, `subprojects.tsv`, сам `files.tsv` — у них своя страница;\n"
"- паспорт `CLAUDE.md`, `README.md`, лицензии (`LICENSE`, `COPYING`,\n"
"  `OFL.txt`), сборочные `.txt` (`CMakeLists.txt`, `requirements.txt`);\n"
"- то, что лежит в скрытых каталогах (кроме `.berth/data/`), в\n"
"  `node_modules`, `build`, `dist`, `vendor`, `target`, `.git`;\n"
"- вложенные репозитории — папки со своим `.git` внутри проекта\n"
"  (референсные исходники, сабмодули): их документы не наши.\n"
"\n"
"Сомневаешься, документ это или код (например, `docs/api.md`,\n"
"сгенерированный из исходников; ноутбук с расчётом) — спроси себя, откроет\n"
"ли его человек, чтобы посмотреть. Откроет — в реестр.\n"
"\n"
"## Два триггера\n"
"\n"
"1. **Ты создал или существенно переписал такой файл** — допиши или\n"
"   обнови его строку тут же, **в том же ходе, до ответа человеку**:\n"
"   отчёт, таблица, презентация, картинка, экспорт — всё это сюда, и\n"
"   неважно, просили тебя про реестр или нет. Не откладывай на потом:\n"
"   следующего хода может не быть, и файл останется без пояснения. Новый\n"
"   файл — новая строка; переписанный — обнови пояснение, если оно\n"
"   перестало быть верным. Переименовал — поправь путь. Удалил — убери\n"
"   строку.\n"
"\n"
"2. **Человек просит описать, что лежит в проекте** («опиши файлы»,\n"
"   «что это за документы», «обнови реестр») — обойди папку по правилам\n"
"   выше, для каждого документа, которого в реестре нет, прочитай начало\n"
"   (первые 30–50 строк; у таблицы — заголовки и пару строк; у картинки —\n"
"   имя и размер, посмотри её, если можешь) и напиши строку.\n"
"   Существующие пояснения **не переписывай** без просьбы, даже если\n"
"   написал бы иначе. Строки на файлы, которых больше нет, убери и\n"
"   скажи об этом в ответе.\n"
"\n"
"   Список кандидатов можно получить так (из корня проекта):\n"
"\n"
"       find . -mindepth 1 \\( -path '*/.*' -o -name node_modules -o -name build -o -name dist -o -name vendor -o -name target -o -exec test -d {}/.git \\; \\) -prune -o \\\n"
"         -type f \\( -name '*.md' -o -name '*.txt' -o -name '*.tsv' -o -name '*.csv' -o -name '*.pdf' -o -name '*.png' -o -name '*.jpg' -o -name '*.docx' -o -name '*.xlsx' -o -name '*.key' -o -name '*.pptx' \\) -print | sort\n"
"\n"
"   плюс `.berth/data/*.tsv`. Из списка убери то, что по правилам не\n"
"   документ (README, LICENSE, CLAUDE.md, CMakeLists.txt).\n"
"\n"
"## В ответе\n"
"\n"
"Коротко: сколько строк добавлено, сколько убрано, что показалось\n"
"черновиком или устаревшим. Сам реестр в ответ не копируй.\n"
"\n"
"Проверь после правки: в каждой строке ровно одна табуляция, каждый путь\n"
"существует, служебных файлов берта и кода в реестре нет.\n"
},
{ "berth-actions",
"---\n"
"name: berth-actions\n"
"description: \"Кнопки над данными в берте — файл .berth/actions.tsv: клик отправляет агенту готовую реплику. Use when the user asks завести/сделать/поправить кнопку, действие или быстрый вызов над таблицей или записью («хочу отмечать просмотренное одним кликом», «сделай кнопку подобрать похожее»), или когда одна и та же просьба про данные повторяется раз за разом. Кнопка заводится в файле, а не описывается в ответе.\"\n"
"---\n"
"\n"
"# Кнопки над данными\n"
"\n"
"Берт показывает таблицы проекта на странице. У раскрытой записи и у\n"
"таблицы целиком он рисует кнопки — действия из `.berth/actions.tsv`. Клик\n"
"собирает реплику из шаблона и отправляет её: либо вкладкой-задачей (правка\n"
"идёт в файл, молча), либо в разговор проекта (ответ надо прочитать).\n"
"\n"
"Действие — это **привязка**, а не процедура. Процедура живёт в скилле,\n"
"действие её зовёт с параметрами записи. Поэтому текст действия короткий.\n"
"\n"
"## Формат\n"
"\n"
"Четыре поля через табуляцию, по строке на действие. Строки с `#` —\n"
"комментарии.\n"
"\n"
"    # область	имя	куда	что сказать\n"
"    запись:.berth/data/films.tsv	Посмотрел	задача	Отметь «{название}» ({год}) просмотренным: оценка {оценка}, впечатление {впечатление}. Дату поставь сегодняшнюю.\n"
"    запись:.berth/data/films.tsv	Похожее	разговор	/watchlist-подбор похожее на «{название}» ({год}, {режиссёр})\n"
"    таблица:.berth/data/films.tsv	Обновить рейтинги	задача	/watchlist-рейтинги\n"
"\n"
"- **область** — `запись:<путь>` (кнопка у раскрытой записи) или\n"
"  `таблица:<путь>` (кнопка у таблицы целиком). Путь — относительно корня\n"
"  проекта, тот же, что в реестре документов.\n"
"- **имя** — подпись кнопки: одно-два слова, глаголом. «Посмотрел»,\n"
"  «Похожее», «Обновить рейтинги».\n"
"- **куда** — `задача`, `разговор` или `команда`. Правило простое:\n"
"  **результат в файле — задача, результат в ответе — разговор**. Задача\n"
"  идёт молча, на простой модели, отдельной вкладкой и не тратит контекст\n"
"  разговора; разговор уводит человека в терминал и показывает ответ.\n"
"  `команда` — без агента вовсе: берт запускает текст через `sh -c` из\n"
"  корня проекта, вывод пишет в `.berth/log/<имя>.log`, таблицу перечитает\n"
"  по mtime. Для готового скрипта (`python3 .berth/mail/mail.py обновить`)\n"
"  это правильная дорога: поднимать агента ради одной строки дорого и\n"
"  медленно. Подстановок `{…}` у команды нет — значения ячеек в оболочку\n"
"  не идут.\n"
"- **раз в N мин** — пятое поле, необязательное, только у `команда`: берт\n"
"  запускает её сам с таким интервалом, пока работает и пока человек\n"
"  что-то делает за окном. Кнопка и расписание — одна строка:\n"
"\n"
"      таблица:.berth/data/inbox.tsv	Обновить	команда	python3 .berth/mail/mail.py обновить	120\n"
"\n"
"  Свежесть таблицы страница показывает сама («обновлено 12 мин назад ·\n"
"  само раз в 2 ч»), писать это в имя кнопки не надо.\n"
"- **что сказать** — одна строка. Подстановки `{имя колонки}` берутся из\n"
"  записи; имя, которого в таблице нет, берт спросит у человека формой\n"
"  перед запуском. Так и делают поля ввода: отдельно их описывать не надо.\n"
"  Пустая ячейка тоже спрашивается — колонка есть, а значения нет.\n"
"\n"
"Файл создай, если его нет (и папку `.berth/`).\n"
"\n"
"## Когда кнопка нужна, а когда нет\n"
"\n"
"Кнопка заводится, если человек набирал бы примерно одну и ту же фразу\n"
"**больше трёх раз**. Разовая просьба — просто сделай её, файл не трогай.\n"
"\n"
"Не заводи кнопки молча «на всякий случай»: страница у записи вмещает\n"
"три-четыре действия, дальше она превращается в панель управления. Если\n"
"кажется, что кнопка пригодится, — предложи одной строкой в ответе.\n"
"\n"
"## Длинный текст — это скилл\n"
"\n"
"Если реплика не влезает в строку, содержит шаги, условия или её захочется\n"
"править отдельно от кнопки — заведи скилл проекта в `.claude/skills` и\n"
"позови его из действия:\n"
"\n"
"    запись:.berth/data/films.tsv	Похожее	разговор	/watchlist-подбор похожее на «{название}»\n"
"\n"
"Так знание живёт в скилле, который человек читает и правит, а кнопка\n"
"остаётся неизменной привязкой.\n"
"\n"
"## Что спросить, когда просят завести кнопку\n"
"\n"
"Коротко, не устраивая допрос:\n"
"\n"
"1. К какой таблице и к чему в ней: к записи или к таблице целиком.\n"
"   Кнопка привязана к **файлу**, а таблиц в проекте бывает несколько —\n"
"   посмотри `.berth/data` и, если их больше одной, уточни. Кнопка над\n"
"   фильмами не появится у таблицы друзей, и наоборот.\n"
"2. Что должно получиться: правка в файле или ответ в разговоре. Отсюда\n"
"   `задача` или `разговор`; есть готовый скрипт без суждений — `команда`.\n"
"3. Чего в записи не хватает — эти имена станут полями формы.\n"
"4. Нужен ли скилл: длинная процедура — да, одна фраза — нет.\n"
"\n"
"Не спрашивай про то, что видно из таблицы: имена колонок прочитай сам.\n"
"\n"
"## Проверь после правки\n"
"\n"
"- в каждой строке ровно три табуляции;\n"
"- путь в области существует и совпадает с путём таблицы;\n"
"- все `{имена}` — либо колонки этой таблицы, либо то, что человек\n"
"  осознанно вводит руками;\n"
"- у одной таблицы не больше трёх-четырёх действий.\n"
"\n"
"В ответе скажи, какая кнопка появилась, где её искать и что она спросит\n"
"перед запуском. Файл в ответ не копируй.\n"
"\n"},
{ "berth-mail-connect",
"---\n"
"name: berth-mail-connect\n"
"description: \"Подключение почтового ящика к берту: один ящик — один проект, чтение через уже настроенный почтовый клиент, IMAP или MCP. Use when the user asks подключить почту, настроить почтовый ящик, добавить второй ящик, «хочу разбирать письма агентом», или когда почта перестала читаться и надо проверить подключение. Отбор писем и задачи из них — не здесь.\"\n"
"---\n"
"\n"
"# Подключение почтового ящика\n"
"\n"
"Довести до того, что **агент читает конкретный ящик**, и оставить один\n"
"известный способ это делать. Отбор писем, задачи из писем, вложения по\n"
"проектам — не здесь: это следующая работа, у неё свои правила.\n"
"\n"
"**Готово — это не «конфиг записан».** Готово — это когда человеку показаны\n"
"пять последних писем и он их узнал.\n"
"\n"
"## Правила, от которых не отступаем\n"
"\n"
"1. **Один ящик — один проект.** Два ящика в одном проекте — это почтовый\n"
"   клиент с переключателем учёток, а не проект берта. У каждого свой\n"
"   каталог, своя таблица, свои правила отбора.\n"
"2. **Пароль — в связке ключей, и ты его не видишь.** Команду с паролем\n"
"   набирает человек в своей оболочке. Никогда не пиши пароль в файл, не\n"
"   проси прислать его в разговор и не подставляй в командную строку: там\n"
"   его видно в `ps`.\n"
"3. **Интерфейс один, способ разный.** Что бы ты ни выбрал — Outlook,\n"
"   IMAP, MCP, — наружу торчит один и тот же `mail.py` с тремя командами\n"
"   (см. «Контракт»). Всё, что будет дальше, знает только их и не знает,\n"
"   как устроено под низом.\n"
"4. **Только стандартная библиотека Python 3.** Никакого `pip`: на рабочей\n"
"   машине установка пакетов — ещё одно согласование, а `imaplib`,\n"
"   `email`, `ssl`, `sqlite3`, `subprocess` есть и так. Считай, что версия\n"
"   3.9 — новее не гарантировано.\n"
"5. **Берт в почту не ходит.** В почту ходишь ты по этим скриптам. Берт\n"
"   только показывает результат и держит кнопки.\n"
"\n"
"## Шаг 1. Что за ящик\n"
"\n"
"Спроси коротко, тремя вопросами, не устраивая допрос:\n"
"\n"
"- **короткое имя** — «Работа», «Х5», «Личная». Так будет называться проект\n"
"  и строка в панели. Адрес именем не делай: `ivan@…` в панели ничего\n"
"  не говорит;\n"
"- **адрес**;\n"
"- **зачем** — одной фразой. Это уедет в паспорт проекта и потом в правила\n"
"  отбора.\n"
"\n"
"Всё остальное узнай сам.\n"
"\n"
"## Шаг 2. Разведка: что уже настроено на машине\n"
"\n"
"**Порядок важен: сначала то, что уже работает.** Настроенный почтовый\n"
"клиент не требует ни пароля, ни согласования у админа — а именно об эти\n"
"две вещи подключение обычно и разбивается.\n"
"\n"
"    ls /Applications | grep -iE \"outlook|mail|thunderbird\"\n"
"    osascript -e 'tell application \"Mail\" to get name of every account'\n"
"    osascript -e 'tell application \"Microsoft Outlook\" to get name of every exchange account'\n"
"    claude mcp list                                      # уже подключённые MCP\n"
"\n"
"Если клиент с этим ящиком уже стоит и синхронизирован — почти наверняка\n"
"это и есть ответ. Переходи к шагу 4, способ «клиент».\n"
"\n"
"**Настроен ли клиент, спрашивай у клиента, а не у его файлов.**\n"
"`~/Library/Mail` и контейнеры Outlook закрыты от терминала защитой macOS\n"
"(TCC): `ls` там отвечает `Operation not permitted`, а с `2>/dev/null` —\n"
"пустотой, которая выглядит точно как «учёток нет». Так уже был сделан\n"
"неверный вывод про настроенный Mail.app с работающим ящиком. Правило:\n"
"stderr у проверок **не глуши**, а «нет доступа» и «нет учёток» —\n"
"разные ответы. Список учёток отдаёт сам клиент через AppleScript;\n"
"первый вызов спросит у человека разрешение на управление приложением\n"
"(окно macOS), `-1743` значит, что его не дали, — попроси нажать\n"
"«Разрешить», это не «не настроен».\n"
"\n"
"**Установлен и настроен — разные вещи.** Пустой список учёток при\n"
"живом приложении — вот что значит «не настроен», и это не тупик: клиент\n"
"можно настроить (шаг 4а). Ящик в списке есть — проверяй наличие\n"
"**писем** в нём, а не значок в `/Applications`.\n"
"\n"
"## Шаг 3. Кто держит почту домена\n"
"\n"
"Провайдер определяется по MX, и это решает, какие пути вообще открыты:\n"
"\n"
"    dig +short MX <домен>\n"
"\n"
"| MX | провайдер | что это значит |\n"
"|---|---|---|\n"
"| `*.google.com` | Google Workspace | IMAP с паролем приложения, если админ не запретил |\n"
"| `*.mail.protection.outlook.com` | Microsoft 365 | **IMAP с обычным паролем отключён Microsoft**, нужен OAuth или клиент |\n"
"| `mx.yandex.*` | Яндекс 360 | IMAP с паролем приложения работает |\n"
"| свой хост | Exchange или postfix | IMAP чаще всего есть, спроси админа порт |\n"
"\n"
"Адрес IMAP-сервера не угадывай — спроси базу автонастройки, ту же, что\n"
"Thunderbird:\n"
"\n"
"    curl -s https://autoconfig.thunderbird.net/v1.1/<домен>\n"
"    curl -s https://autoconfig.<домен>/mail/config-v1.1.xml\n"
"\n"
"В ответе `<incomingServer type=\"imap\">` — хост, порт и тип шифрования.\n"
"Если базы нет, попробуй `imap.<домен>` и `mail.<домен>` на порту 993.\n"
"\n"
"## Шаг 4. Выбор способа\n"
"\n"
"Иди сверху вниз, бери первый, который сработал.\n"
"\n"
"### а) Через настроенный клиент (Outlook, Mail.app)\n"
"\n"
"Лучший путь для рабочей почты: ноль паролей, ноль согласований, ящик уже\n"
"синхронизирован. Читаем AppleScript'ом.\n"
"\n"
"Сначала **прочитай словарь**, а не полагайся на память — у Outlook он\n"
"менялся, и в «новом Outlook» часть свойств выпилена:\n"
"\n"
"    sdef \"/Applications/Microsoft Outlook.app\" > /tmp/outlook.sdef\n"
"    grep -oE '<(class|property|element)[^>]*name=\"[^\"]+\"' /tmp/outlook.sdef | head -60\n"
"\n"
"Проверка связи:\n"
"\n"
"    osascript -e 'tell application \"Microsoft Outlook\" to get name of inbox'\n"
"\n"
"Первый запуск покажет системный запрос на управление Outlook — человек\n"
"должен нажать «Разрешить», иначе будет ошибка -1743.\n"
"\n"
"**Запрос назовёт не берт, а приложение, с которого началась цепочка.**\n"
"macOS выдаёт это разрешение «ответственному» процессу — ближайшему\n"
"предку-приложению, — а берт, запущенный из терминала, приложением не\n"
"считается: разрешение уйдёт терминалу (Warp, Terminal, iTerm). Это не\n"
"ошибка, всё будет работать. Но если человек пробовал из терминала, а\n"
"работать станет из Berth.app, запрос придёт второй раз — уже от имени\n"
"берта, и это тоже нормально. Скажи об этом заранее, иначе второй диалог\n"
"выглядит как поломка.\n"
"\n"
"Выборку всегда делай через `whose`, а не перебором: в ящике бывают\n"
"десятки тысяч писем, и `messages of inbox` целиком встанет колом.\n"
"\n"
"    tell application \"Microsoft Outlook\"\n"
"      set рубеж to (current date) - 3 * days\n"
"      set найденные to (messages of inbox whose time received > рубеж)\n"
"    end tell\n"
"\n"
"У сообщения обычно есть `subject`, `sender`, `time received`, `is read`,\n"
"`plain text content`, `id` — **проверь по словарю**, что есть в этой\n"
"версии, и не выдумывай свойств.\n"
"\n"
"Если в приложении включён «новый Outlook» и словарь скудный — скажи\n"
"человеку, что можно переключиться на классический (в самом Outlook,\n"
"тумблер «New Outlook»), либо переходи к варианту (в).\n"
"\n"
"**Настроенного клиента нет — это не тупик, а первый шаг.** Для рабочей\n"
"почты он обычно короче, чем пароль приложения: вход идёт через OAuth\n"
"самой компании, и согласовывать с админом нечего. Попроси человека\n"
"подключить ящик в macOS: Системные настройки → «Учётные записи\n"
"интернета» → Google (или Exchange, или «Другая учётная запись почты») →\n"
"войти рабочей учёткой и оставить галочку «Почта». Mail.app увидит ящик\n"
"сразу; первая синхронизация занимает несколько минут, и до её конца\n"
"выборка по датам вернёт неполный список — скажи об этом заранее.\n"
"\n"
"Дальше всё то же, что у Outlook, только словарь свой:\n"
"\n"
"    osascript -e 'tell application \"Mail\" to get name of every account'\n"
"\n"
"    tell application \"Mail\"\n"
"      set рубеж to (current date) - 3 * days\n"
"      set найденные to (messages of inbox whose date received > рубеж)\n"
"    end tell\n"
"\n"
"У сообщения Mail.app это `subject`, `sender`, `date received`,\n"
"`read status`, `content`, `id`, `mail attachment` — **сверь по словарю**\n"
"(`sdef /System/Applications/Mail.app`), свойства у Mail и Outlook\n"
"называются по-разному (`date received` против `time received`,\n"
"`read status` против `is read`).\n"
"\n"
"### б) IMAP с паролем приложения\n"
"\n"
"Работает у Google, Яндекса, Mail.ru и почти любого своего сервера. Не\n"
"работает у Microsoft 365 — там обычные пароли для IMAP отключены на\n"
"стороне Microsoft, и никакой пароль приложения этого не обойдёт.\n"
"\n"
"Человеку надо завести **пароль приложения** (не основной пароль!) в\n"
"настройках безопасности своей почты. Часто это требует включённой\n"
"двухфакторной аутентификации, а у корпоративной учётки это сплошь и\n"
"рядом запрещено админом: раздела «Пароли приложений» просто нет.\n"
"\n"
"Не отправляй человека к админу первым делом — это недели. Вернись к (а)\n"
"и предложи **настроить клиент с нуля**: вход в «Учётных записях\n"
"интернета» идёт по OAuth, пароль приложения там не нужен вовсе, и\n"
"корпоративная политика ему обычно не мешает — почту в Mail.app компании\n"
"разрешают чаще, чем IMAP по паролю.\n"
"\n"
"### в) Локальная база клиента\n"
"\n"
"Крайний случай, когда AppleScript урезан, а IMAP закрыт. У Outlook это\n"
"\n"
"    ~/Library/Group Containers/UBF8T346G9.Office/Outlook/Outlook 15 Profiles/Main Profile/Data/Outlook.sqlite\n"
"\n"
"Схема недокументирована и меняется между версиями, поэтому: **только\n"
"чтение**, начни с `.tables` и `.schema`, и честно скажи человеку, что\n"
"путь хрупкий и может отвалиться после обновления Office. Терминалу\n"
"понадобится «Полный доступ к диску» в настройках приватности.\n"
"\n"
"### г) MCP-сервер или коннектор\n"
"\n"
"Красивый путь, но упирается в регистрацию приложения у администратора\n"
"(Google Cloud OAuth client, Microsoft app registration). Если у человека\n"
"это уже согласовано — отлично, `claude mcp add`. Если нет — не начинай с\n"
"него: согласование занимает недели, а (а) работает сегодня.\n"
"\n"
"## Шаг 5. Секрет — руками человека\n"
"\n"
"Только для способа (б). **Ты этой команды не выполняешь.** Попроси\n"
"человека открыть в берте новую оболочку (⌘T) и набрать там:\n"
"\n"
"    security add-generic-password -s berth-mail -a \"<логин>\" -w\n"
"\n"
"Флаг `-w` без значения означает «спроси пароль у терминала»: он\n"
"вводится вслепую, не попадает ни в историю команд, ни в этот разговор,\n"
"ни в `ps`.\n"
"\n"
"Скрипт достаёт его сам:\n"
"\n"
"    security find-generic-password -s berth-mail -a \"<логин>\" -w\n"
"\n"
"Первое обращение покажет запрос связки ключей — человеку надо нажать\n"
"«Всегда разрешать», иначе запрос будет вылезать при каждом чтении почты.\n"
"\n"
"Служба всегда `berth-mail`, учётка — логин ящика. Поэтому два ящика не\n"
"сталкиваются.\n"
"\n"
"## Шаг 6. Контракт: `mail.py`\n"
"\n"
"Положи в `<проект>/.berth/mail/mail.py`. Ровно пять команд, ровно эти\n"
"имена — на них опирается всё, что появляется дальше (кнопки, таймер,\n"
"разбор по скиллу `berth-mail-triage`):\n"
"\n"
"    mail.py список [--дней N] [--папка ИМЯ] [--макс N]   → TSV в stdout\n"
"    mail.py письмо <id>                                   → текст письма\n"
"    mail.py вложения <id> <каталог>                       → сохраняет, печатает пути\n"
"    mail.py обновить [--дней N] [--макс N] [--файл ПУТЬ]  → дописывает в таблицу\n"
"    mail.py пометить <id> колонка=значение … [--файл ПУТЬ] → правит ячейки строки\n"
"\n"
"`список` печатает TSV с этой строкой заголовков и в этом порядке:\n"
"\n"
"    дата	от	тема	фрагмент	вложения	прочитано	id\n"
"\n"
"- `дата` — `2026-09-08 09:14`, местное время. Такой формат сортируется\n"
"  как строка, и берт покажет его как есть;\n"
"- `от` — `Иван Петров <ivan@…>`, а если имени нет — только адрес;\n"
"- `фрагмент` — первые ~200 знаков текста, в одну строку;\n"
"- `вложения` — имена через запятую, пусто если нет;\n"
"- `прочитано` — `да` / `нет`;\n"
"- `id` — Message-ID без угловых скобок. Он должен быть **одинаковым от\n"
"  запуска к запуску**: по нему потом ищут письмо и отбрасывают повторы.\n"
"\n"
"Обязательное: табуляции и переводы строк внутри значений заменяй\n"
"пробелом, иначе TSV поедет. По умолчанию `--дней 3`, `--макс 200`.\n"
"При ошибке — одна понятная строка в stderr и ненулевой код возврата.\n"
"\n"
"Умолчания и хост не зашивай в код — читай `.berth/mail.conf` (шаг 8).\n"
"\n"
"### `обновить` — та же добыча, но в файл\n"
"\n"
"`список` живёт ровно один ответ агента: напечатал и забыл. `обновить`\n"
"кладёт те же письма в таблицу `.berth/data/inbox.tsv` (её показывает\n"
"страница проекта) и печатает в stdout только итог — сколько добавлено и\n"
"сколько всего. Никаких суждений он не делает: важность и маршрут — работа\n"
"агента, а притащить письма должен цикл в скрипте, а не разговор.\n"
"\n"
"Требования, каждое — из грабель:\n"
"\n"
"- **повторы отбрасываются по `id`**. Он для того и обязан быть стабильным\n"
"  от запуска к запуску;\n"
"- **без `--дней` окно считается от таблицы, а не от календаря**: берётся\n"
"  самая поздняя `дата` в файле, от неё с нахлёстом в сутки (повторы всё\n"
"  равно отбросятся по id). Иначе `--дней 7`, не запускавшийся восемь дней,\n"
"  молча теряет письма — а на этой команде будет стоять кнопка и таймер.\n"
"  У пустой таблицы окно — «дней» из `mail.conf`. Явный `--дней N`\n"
"  по-прежнему берёт ровно N дней. Итог печатай с датой, от которой качал:\n"
"  `с 2026-09-10 09:39, добавлено 3, всего 52`;\n"
"- **выборка упёрлась в `--макс`** — предупреждение в stderr: старее могли\n"
"  остаться письма, и нахлёст их уже не спасёт;\n"
"- **существующие строки не трогаются вовсе** — даже если письмо успели\n"
"  прочитать. Строка принадлежит человеку и агенту с того мгновения, как\n"
"  попала в файл;\n"
"- **незнакомые колонки едут через обновление нетронутыми.** Агент\n"
"  дописывает к строкам свои колонки суждения (`важность`, `проект`, `что\n"
"  делать`), и скрипт обязан их пронести. Отсюда правило: состав и порядок\n"
"  колонок диктует **файл**, а не скрипт. Новое письмо раскладывается по\n"
"  тем колонкам, которые в файле есть; колонки суждения у него пустые;\n"
"- **колонки `id` нет — это отказ**, а не повод писать заново: без неё\n"
"  повторы не отличить, и молчаливое обновление удвоит таблицу;\n"
"- файла нет — создаётся с колонками-фактами и **BOM** в начале (его ждёт\n"
"  и Excel, и разбор таблиц в берте);\n"
"- запись — через временный файл и `rename`: обрыв на середине не должен\n"
"  оставить от таблицы огрызок.\n"
"\n"
"Чем проверить: два запуска подряд без `--дней` — первый печатает «с\n"
"<последняя дата в файле>», второй добавляет ноль; дописанная в файл руками\n"
"колонка переживает обновление; письмо, пришедшее между запусками,\n"
"появляется в конце.\n"
"\n"
"### `пометить` — колонки суждения по id\n"
"\n"
"Разбор писем (скилл `berth-mail-triage`) пишет в строку письма `статус`\n"
"и `что делать`. Править TSV руками или sed'ом нельзя — табуляции, BOM и\n"
"полуписанный файл, — поэтому запись идёт одной командой:\n"
"\n"
"    mail.py пометить <id> статус=задача \"что делать=ответить до пятницы\"\n"
"\n"
"- строка ищется по `id`; нет такой — отказ с ненулевым кодом;\n"
"- колонки, которых в заголовке нет, добавляются в конец, а строки короче\n"
"  заголовка дописываются пустыми ячейками — рваные хвосты разные\n"
"  программы читают по-разному;\n"
"- табуляции и переводы строк в значении заменяются пробелом;\n"
"- запись через временный файл и `rename`, BOM сохраняется;\n"
"- печатает `помечено <id>: статус=…`.\n"
"\n"
"Берт заводит `статус` и `что делать` в заголовке `inbox.tsv` вместе с\n"
"проектом; `обновить` оставляет их у новых писем пустыми.\n"
"\n"
"## Шаг 7. Проверка на живом\n"
"\n"
"Обязательный шаг, без него работа не сделана:\n"
"\n"
"    python3 .berth/mail/mail.py список --дней 3 --макс 5\n"
"\n"
"Покажи человеку эти пять строк и спроси прямо: **это его письма?** Затем\n"
"проверь вторую команду на любом из них (`mail.py письмо <id>`) — часто\n"
"список работает, а тело письма падает на кодировках или multipart.\n"
"\n"
"Что проверить в выводе своими глазами: не поехали ли колонки, не пустые\n"
"ли `от` и `тема`, не превратилась ли кириллица в `=?utf-8?B?…` (значит,\n"
"забыл `email.header.decode_header`).\n"
"\n"
"### Таблица на странице\n"
"\n"
"Человек подтвердил, что ящик тот, — заполни таблицу, ради которой всё\n"
"делалось:\n"
"\n"
"    python3 .berth/mail/mail.py обновить --дней 7\n"
"\n"
"Семь дней — только для первого заполнения: дальше команда без `--дней`\n"
"сама продолжает с последней даты в таблице.\n"
"\n"
"Берт заводит её вместе с проектом: `.berth/data/inbox.tsv` с одной строкой\n"
"заголовков (семь колонок `список` плюс `статус` и `что делать` для\n"
"разбора), строка показа в `.berth/shown.tsv` — на странице видны «дата ·\n"
"от · тема · статус · что делать», остальное в раскрытой записи — и\n"
"строка в реестре `.berth/files.tsv`. После `обновить`\n"
"письма появляются на странице сами. Файл не пересоздавай и не\n"
"переименовывай: пустая таблица с заголовками — не ошибка, а заготовка.\n"
"\n"
"Если тебя позвали не из берта и этих файлов нет — заведи их сам, ровно\n"
"такими:\n"
"\n"
"    .berth/data/inbox.tsv  дата<TAB>от<TAB>тема<TAB>фрагмент<TAB>вложения<TAB>прочитано<TAB>id<TAB>статус<TAB>что делать\n"
"    .berth/shown.tsv   show<TAB>.berth/data/inbox.tsv<TAB>дата|от|тема|статус|что делать|-фрагмент|-вложения|-прочитано|-id\n"
"                       filter<TAB>.berth/data/inbox.tsv<TAB>статус<TAB>пусто<TAB>не разобрано\n"
"    .berth/files.tsv   .berth/data/inbox.tsv<TAB>Входящие письма: дата, от кого, тема, фрагмент\n"
"    .berth/actions.tsv таблица:.berth/data/inbox.tsv<TAB>Обновить<TAB>команда<TAB>python3 .berth/mail/mail.py обновить<TAB>120\n"
"                       таблица:.berth/data/inbox.tsv<TAB>Разобрать<TAB>разговор<TAB>/berth-mail-triage\n"
"\n"
"Кнопка «Обновить» — дорога `команда`: берт запускает скрипт сам, без\n"
"агента, и перечитывает таблицу; последнее поле — раз в сколько минут он\n"
"делает это без клика. «Разобрать» открывает разбор в разговоре по скиллу\n"
"`berth-mail-triage`. Других кнопок над письмами не заводи.\n"
"\n"
"Кнопки-действия над таблицей не заводи: что человек захочет делать с\n"
"письмами, выяснится, когда он их увидит.\n"
"\n"
"## Шаг 8. Что записать\n"
"\n"
"**Проект** — подпапка в `~/.config/berth/Интеграции/<короткое имя>`.\n"
"Отдельная группа, а не папка рядом с рабочими проектами: ящиков бывает\n"
"несколько, вместе они читаются как одно, а порознь — как шум среди\n"
"настоящей работы. Заодно у человека не приходится спрашивать «куда\n"
"положить»: у подключения и так довольно вопросов.\n"
"\n"
"Обычно и папку, и группу заводит сам берт: «+» у группы «Интеграции» →\n"
"«Почта» → адрес, и разговор начинается уже внутри готового проекта. Тогда\n"
"делать по этому абзацу нечего — ты уже в нужном каталоге. Если же тебя\n"
"позвали из другого места, заведи папку сам, а группу подключи той же\n"
"строкой, какой человек добавляет свои: `add<TAB>/Users/<…>/.config/berth/\n"
"Интеграции<TAB>интеграции` в `~/.config/berth/groups.tsv` (третье поле —\n"
"вид группы, по нему «+» открывает подключение, а не новый проект).\n"
"\n"
"    <проект>/CLAUDE.md              чей ящик, зачем, каким способом читается\n"
"    <проект>/.berth/mail.conf       как дотянуться (без секретов)\n"
"    <проект>/.berth/mail/mail.py    чтение\n"
"    <проект>/Вложения/              каталог по умолчанию для файлов из писем\n"
"\n"
"Почтовый проект — **обычный проект берта**: у него есть документы, реестр,\n"
"действия и задачи, и ничего особенного в нём нет. `Вложения/` — умолчание,\n"
"а не правило: `mail.py вложения` принимает каталог аргументом, и куда\n"
"сохранять конкретный файл, решает тот, кто зовёт, — действие, которое\n"
"человек завёл сам, или суждение агента по его правилам.\n"
"\n"
"Чего действительно делать нельзя — **тащить в проект копию письма**. Письмо\n"
"живёт в ящике, задача на него ссылается строкой. Копии превратили бы проект\n"
"во второй почтовый ящик, а вложение — это просто файл, которому надо где-то\n"
"лежать.\n"
"\n"
"`mail.conf` — по строке `ключ = значение`, как у настроек берта:\n"
"\n"
"    имя = Работа\n"
"    адрес = ivan@example.com\n"
"    способ = outlook          # outlook | mail.app | imap | sqlite | mcp\n"
"    учётка = Работа           # имя учётки в клиенте, для способа outlook\n"
"    сервер = imap.example.com # для imap\n"
"    порт = 993\n"
"    логин = ivan@example.com\n"
"    секрет = berth-mail       # служба в связке ключей, учётка = логин\n"
"    папка = INBOX\n"
"    проверено = 2026-09-08 09:14\n"
"\n"
"**Отдельного реестра подключённого нет.** Группа «Интеграции» и есть\n"
"реестр: берт знает её проекты, а правда про каждое подключение лежит в\n"
"его `mail.conf`. Второй список пришлось бы держать в согласии с первым, и\n"
"разошлись бы они на первом же переносе папки.\n"
"\n"
"## Второй и третий ящик\n"
"\n"
"Ради этого всё и разделено. Что делать, когда просят добавить ещё один:\n"
"\n"
"- **новый проект, не новая папка внутри старого.** Правила отбора у\n"
"  рабочей и личной почты разные, и жить они должны порознь;\n"
"- **разведку повторяй, выбор способа — нет.** Если первый ящик того же\n"
"  провайдера уже читается, возьми тот же способ и спроси только логин;\n"
"- **у Outlook укажи учётку явно.** Когда в клиенте несколько ящиков,\n"
"  скрипт без имени учётки прочитает не тот: `inbox` вернёт папку\n"
"  учётки по умолчанию. Разложи по именам учёток из словаря;\n"
"- **связка ключей их различает сама** — учётка это логин;\n"
"- **не заводи «общую» таблицу на все ящики.** Сведёт их вместе тот, кто\n"
"  захочет: срез — дело одной команды.\n"
"\n"
"## Если не вышло\n"
"\n"
"| что видно | что это значит |\n"
"|---|---|\n"
"| `AUTHENTICATIONFAILED` у Google | нужен пароль приложения, обычный пароль IMAP не пускает |\n"
"| `LOGIN failed` / `basic auth disabled` у M365 | Microsoft отключил обычные пароли, путь (б) закрыт — иди в (а) |\n"
"| `execution error … (-1743)` | не выдано разрешение на управление приложением: Системные настройки → Конфиденциальность → Автоматизация |\n"
"| AppleScript висит минутами | забыл `whose`, перебираешь весь ящик |\n"
"| `=?utf-8?B?…` в теме | не раскодировал заголовок (`decode_header`) |\n"
"| пустое тело письма | письмо только в HTML — бери `text/html` и вычищай теги |\n"
"| `unable to open database file` у sqlite | терминалу нужен «Полный доступ к диску» |\n"
"\n"
"Если уперлись в запрет админа — так и скажи. Это не техническая\n"
"проблема, и обходить её скриптами не надо: человеку нужно знать, что\n"
"дальше идти к администратору, а не что «почему-то не работает».\n"
"\n"
"## Чего этот скилл не делает\n"
"\n"
"Отбор важного, задачи из писем, раскладка вложений по проектам,\n"
"расписание — **не здесь**. Это отдельная работа поверх готового\n"
"`mail.py`, и правила у неё живут в скилле проекта, который человек\n"
"читает и правит. Закончив подключение, скажи об этом одной строкой:\n"
"дальше человек просит «разбери почту», и это уже другой разговор.\n"
"\n"
"## В ответе\n"
"\n"
"Коротко: какой ящик подключён, каким способом, где лежит проект, какой\n"
"командой проверить руками. Скрипт целиком в ответ не копируй — он лежит\n"
"в файле. Если что-то осталось на человеке (нажать «Разрешить», завести\n"
"пароль приложения, сходить к админу) — назови это отдельной строкой,\n"
"чтобы не потерялось.\n"},
{ "berth-mail-triage",
"---\n"
"name: berth-mail-triage\n"
"description: \"Разбор входящих писем в берте вместе с человеком: пачками, с предложенным решением, статус в таблицу, задачи в проекты. Use when the user asks разобрать почту, «что там в почте», «разбери письма», нажал кнопку «Разобрать» над таблицей писем, или спрашивает, что делать с письмом. Подключение ящика — не здесь (berth-mail-connect).\"\n"
"---\n"
"\n"
"# Разбор почты вместе с человеком\n"
"\n"
"Письма лежат в `.berth/data/inbox.tsv` почтового проекта, их приносит\n"
"`mail.py обновить` (по кнопке и по таймеру). Разбор — это решение, что с\n"
"каждым письмом делать, и решает **человек**: ты предлагаешь, он\n"
"подтверждает. Молча размечать нельзя — статус, поставленный без него,\n"
"он не увидит и не поправит.\n"
"\n"
"Результат разбора живёт в двух колонках той же таблицы:\n"
"\n"
"- `статус` — пусто (не разобрано), `задача`, `к сведению`,\n"
"  `ждём ГГГГ-ММ-ДД` (ответ за кем-то, после даты напомнить);\n"
"- `что делать` — одна короткая фраза, до шести слов: «ответить до\n"
"  пятницы», «согласовать бюджет», «ничего».\n"
"\n"
"Обе пишутся только командой `mail.py пометить <id> статус=… \"что\n"
"делать=…\"`: она находит строку по id и переписывает файл целиком через\n"
"временный, колонок нет — добавит. Руками TSV не правь.\n"
"\n"
"Страница проекта показывает таблицу и считает строки с пустым статусом:\n"
"разбор закончен, когда таких ноль.\n"
"\n"
"## Порядок\n"
"\n"
"1. **Освежи и отбери.** Запусти `python3 .berth/mail/mail.py обновить`\n"
"   (без `--дней` — продолжит с последней даты). Если у ящика есть свой\n"
"   скилл отбора (например `разбери-почту` с правилами в `важное.conf`) —\n"
"   запусти его: он покажет, что важно по правилам человека, и это будет\n"
"   первая пачка. Правила ящика важнее твоих догадок.\n"
"2. **Возьми только неразобранное:** строки, где `статус` пуст.\n"
"\n"
"       awk -F'\\t' 'NR==1{for(i=1;i<=NF;i++)c[$i]=i} NR>1&&$c[\"статус\"]==\"\"' .berth/data/inbox.tsv\n"
"\n"
"3. **Сгруппируй в пачки** не больше пяти: тред (одна тема с `Re:`),\n"
"   один отправитель, рассылки и уведомления, письма с вложениями, «за\n"
"   вчера». Порядок: важное по правилам ящика, потом личные письма, потом\n"
"   автоматы.\n"
"4. **По каждой пачке предложи решение**, а не задавай открытый вопрос.\n"
"   Одна реплика — одна пачка, коротко:\n"
"\n"
"       За вчера 4 письма с вложениями от бухгалтерии. Предлагаю все\n"
"       к сведению, кроме счёта от 9 сентября — его в задачу «оплатить\n"
"       до 15.09». Ок?\n"
"\n"
"   Человек отвечает одним словом или поправкой. Не переспрашивай то,\n"
"   что видно из таблицы; не показывай все письма списком — за этим\n"
"   человек откроет страницу.\n"
"5. **После ответа сразу пиши**: `пометить` каждой строке пачки, задачи —\n"
"   в файл (ниже). Потом следующая пачка. Не копи решения до конца:\n"
"   оборванный разговор не должен терять уже сказанное.\n"
"6. **В конце одна строка**: сколько в задачи, сколько к сведению,\n"
"   сколько ждём, и что осталось неразобранным, если человек прервал.\n"
"\n"
"## Задачи из писем\n"
"\n"
"Задача идёт в `.berth/tasks.md` **того проекта, к которому письмо\n"
"относится**, по формату скилла `berth-tasks`, а не в почтовый проект.\n"
"Список проектов человека берт держит в `~/.config/berth/projects.tsv`\n"
"(или `$BERTH_HOME/.config/berth/projects.tsv`): путь, имя, группа. Не\n"
"уверен, куда, — спроси в той же реплике, где предлагаешь решение\n"
"(«в DepTrans?»). Совсем не про проекты — в почтовый.\n"
"\n"
"В описании задачи первой строкой ссылка на письмо, чтобы его нашли:\n"
"\n"
"    ## Прокомментировать сравнение с конкурентом\n"
"    Письмо: 2026-09-09 14:02 · Ивин · «Сравнение с X» · id CAK…@mail.gmail.com\n"
"    Ящик: ~/Berth_integrations/Почта ivan@example.com\n"
"    Просит до пн 15.09; файлы во вложении письма.\n"
"\n"
"Копию письма в проект не тащи: письмо живёт в ящике, задача на него\n"
"ссылается. Вложения — только если человек попросил: `mail.py вложения\n"
"<id> <папка>`, по умолчанию `Вложения/` почтового проекта.\n"
"\n"
"Ответ в треде, где задача уже есть (та же тема без `Re:` в `tasks.md`\n"
"или в `tasks-done.md`), — не вторая задача: допиши строку в описание\n"
"существующей и поставь письму `статус=задача`.\n"
"\n"
"## Порог\n"
"\n"
"Не всякое письмо — задача. Задача — когда от человека ждут действия с\n"
"результатом: ответить, сделать, решить, оплатить. Рассылка, уведомление,\n"
"«спасибо», приглашение на встречу (её видит календарь) — `к сведению`.\n"
"Сомневаешься — предложи «к сведению», человек поправит.\n"
"\n"
"`ждём` — когда действие за другим человеком, а этому надо не забыть:\n"
"поставь дату, после которой напомнить, и ничего больше не заводи.\n"
"\n"
"## Чего не делать\n"
"\n"
"- Не размечай без подтверждения человека, даже «очевидное».\n"
"- Не переименовывай и не пересоздавай `inbox.tsv`, не вырезай из него\n"
"  разобранные строки: они уйдут с глаз фильтром страницы, а `обновить`\n"
"  вернуло бы вырезанное обратно.\n"
"- Не заводи вторую таблицу для разобранного.\n"
"- Не читай тело каждого письма подряд ради разбора: фрагмента и темы\n"
"  обычно хватает; тело — `mail.py письмо <id>` — только там, где от\n"
"  него зависит решение.\n"},
};

const int SKILLS_BUILTIN_COUNT = (int)(sizeof(SKILLS_BUILTIN) / sizeof(SKILLS_BUILTIN[0]));
