// Встроенные скиллы берта: текст SKILL.md, который при первом запуске
// раскладывается в каталог ~/.config/berth/skills/<имя>/. Здесь, а не
// файлом рядом с бинарём, потому что бинарь ходит и без папки assets.
#include "skills_builtin.h"

const BuiltinSkill SKILLS_BUILTIN[] = {
{ "berth-tasks",
"---\n"
"name: berth-tasks\n"
"description: \"Задачи проекта в берте — файл .berth/tasks.md. Use when the user asks завести/добавить/записать задачи, составить список задач или план работ, поправить или переформулировать задачу, отметить сделанное, взять задачу в работу, или когда просят «запиши это как задачу». Задачи заводятся в файле, а не текстом в ответе.\"\n"
"---\n"
"\n"
"# Задачи проекта в берте\n"
"\n"
"Список задач проекта живёт в файле `.berth/tasks.md` в корне проекта. Его\n"
"показывает берт на странице проекта: там задачи раскрывают, переставляют,\n"
"отправляют в работу и отмечают сделанными. Файл правится и руками, и тобой.\n"
"\n"
"**Главное правило:** когда человек просит завести задачи, составить список\n"
"задач или план работ, записать что-то «как задачу» — задачи идут **в файл**,\n"
"а не списком в ответе. Список в ответе человек не увидит на странице и не\n"
"сможет отправить в работу. В ответе — коротко, что записано.\n"
"\n"
"Если файла нет — создай его (и папку `.berth/`, если её нет).\n"
"У подпроекта свой файл в своей папке: задачи подпроекта — в\n"
"`<подпроект>/.berth/tasks.md`, а не у родителя.\n"
"\n"
"## Формат\n"
"\n"
"Формат жёсткий — его разбирает берт:\n"
"\n"
"    Текст до первой задачи — заметки человека, сохраняй как есть.\n"
"\n"
"    ## Название задачи\n"
"    Описание в свободной форме. Абзацы через пустую строку; одиночный\n"
"    перевод строки склеивается в абзац, как в Markdown.\n"
"\n"
"    ## Вторая задача без описания\n"
"\n"
"    ## [?] Сделанная, но человек ещё не проверил\n"
"\n"
"    ## [x] Сделанная и проверенная\n"
"\n"
"- Заголовок `## ` — одна задача. Пометки состояния: без пометки — открыта,\n"
"  `[?]` — сделана, ждёт проверки, `[x]` — сделана.\n"
"- Порядок задач в файле — порядок работы: первая — самая важная. Новые\n"
"  открытые задачи ставь **после последней открытой и перед сделанными**,\n"
"  если человек не сказал, что это срочно.\n"
"- Название — одна строка, до 300 байт. Описание — до 8 КБ; это ТЗ, а не\n"
"  заметка: причина, что сделать, какие файлы, чем проверить.\n"
"- Внутри описания не используй строки, начинающиеся с `## ` — берт примет\n"
"  их за новую задачу. Заголовки внутри описания — `###` или жирным.\n"
"- Всё остальное в файле не трогай: чужие задачи, их порядок и текст\n"
"  человека до первой задачи.\n"
"\n"
"## Что делать по просьбе\n"
"\n"
"- **Завести задачи / составить план** — разбей работу на задачи размером в\n"
"  один осмысленный кусок (час-два работы), у каждой название и описание,\n"
"  допиши в файл. В ответе перечисли названия одной строкой каждое.\n"
"- **Поправить задачу** — найди её по заголовку дословно и перепиши текст;\n"
"  пометку состояния сохрани.\n"
"- **Взять задачу в работу** — просто делай её; когда закончишь, замени\n"
"  `## Название` на `## [?] Название`. Сразу `[x]` не ставь: проверка —\n"
"  дело человека, если он не просил иначе.\n"
"- **Отметить сделанное** по просьбе человека — `[x]`.\n"
"- **Задача пришла от берта** — в её тексте после `---` стоит наказ, какую\n"
"  пометку поставить по завершении; следуй ему.\n"
"\n"
"Проверь после правки: файл начинается как раньше, каждая задача — один\n"
"заголовок `## `, число задач сходится с тем, что ты собирался записать.\n"
},
};

const int SKILLS_BUILTIN_COUNT = (int)(sizeof(SKILLS_BUILTIN) / sizeof(SKILLS_BUILTIN[0]));
