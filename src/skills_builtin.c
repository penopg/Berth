// Встроенные скиллы берта: текст SKILL.md, который при первом запуске
// раскладывается в каталог ~/.config/berth/skills/<имя>/. Здесь, а не
// файлом рядом с бинарём, потому что бинарь ходит и без папки assets.
#include "skills_builtin.h"

const BuiltinSkill SKILLS_BUILTIN[] = {
{ "berth-tasks",
"---\n"
"name: berth-tasks\n"
"description: \"Задачи проекта в берте — файл .berth/tasks.md. Use when the user asks завести/добавить/записать задачи, составить список задач или план работ, поправить или переформулировать задачу, отметить сделанное, взять задачу в работу, или когда просят «запиши это как задачу». Задачи заводятся в файле, а не текстом в ответе.\"\n"
"---\n"
"\n"
"# Задачи проекта в берте\n"
"\n"
"Список задач проекта живёт в файле `.berth/tasks.md` в корне проекта. Его\n"
"показывает берт на странице проекта: там задачи раскрывают, переставляют,\n"
"отправляют в работу и отмечают сделанными. Файл правится и руками, и тобой.\n"
"\n"
"**Главное правило:** когда человек просит завести задачи, составить список\n"
"задач или план работ, записать что-то «как задачу» — задачи идут **в файл**,\n"
"а не списком в ответе. Список в ответе человек не увидит на странице и не\n"
"сможет отправить в работу. В ответе — коротко, что записано.\n"
"\n"
"Если файла нет — создай его (и папку `.berth/`, если её нет).\n"
"У подпроекта свой файл в своей папке: задачи подпроекта — в\n"
"`<подпроект>/.berth/tasks.md`, а не у родителя.\n"
"\n"
"## Формат\n"
"\n"
"Формат жёсткий — его разбирает берт:\n"
"\n"
"    Текст до первой задачи — заметки человека, сохраняй как есть.\n"
"\n"
"    ## Название задачи\n"
"    Описание в свободной форме. Абзацы через пустую строку; одиночный\n"
"    перевод строки склеивается в абзац, как в Markdown.\n"
"\n"
"    ## Вторая задача без описания\n"
"\n"
"    ## [?] Сделанная, но человек ещё не проверил\n"
"\n"
"    ## [x] Сделанная и проверенная\n"
"\n"
"- Заголовок `## ` — одна задача. Пометки состояния: без пометки — открыта,\n"
"  `[?]` — сделана, ждёт проверки, `[x]` — сделана.\n"
"- Порядок задач в файле — порядок работы: первая — самая важная. Новые\n"
"  открытые задачи ставь **после последней открытой и перед сделанными**,\n"
"  если человек не сказал, что это срочно.\n"
"- Название — одна строка, до 300 байт. Описание — до 8 КБ; это ТЗ, а не\n"
"  заметка: причина, что сделать, какие файлы, чем проверить.\n"
"- Внутри описания не используй строки, начинающиеся с `## ` — берт примет\n"
"  их за новую задачу. Заголовки внутри описания — `###` или жирным.\n"
"- Всё остальное в файле не трогай: чужие задачи, их порядок и текст\n"
"  человека до первой задачи.\n"
"\n"
"## Что делать по просьбе\n"
"\n"
"- **Завести задачи / составить план** — разбей работу на задачи размером в\n"
"  один осмысленный кусок (час-два работы), у каждой название и описание,\n"
"  допиши в файл. В ответе перечисли названия одной строкой каждое.\n"
"- **Поправить задачу** — найди её по заголовку дословно и перепиши текст;\n"
"  пометку состояния сохрани.\n"
"- **Взять задачу в работу** — просто делай её; когда закончишь, замени\n"
"  `## Название` на `## [?] Название`. Сразу `[x]` не ставь: проверка —\n"
"  дело человека, если он не просил иначе.\n"
"- **Отметить сделанное** по просьбе человека — `[x]`.\n"
"- **Задача пришла от берта** — в её тексте после `---` стоит наказ, какую\n"
"  пометку поставить по завершении; следуй ему.\n"
"\n"
"Проверь после правки: файл начинается как раньше, каждая задача — один\n"
"заголовок `## `, число задач сходится с тем, что ты собирался записать.\n"
},
{ "berth-data",
"---\n"
"name: berth-data\n"
"description: \"Данные проекта в берте — таблицы .berth/data/<имя>.tsv и срезы .berth/views.tsv. Use when the user asks собрать, дополнить, сравнить или отсортировать список однотипных вещей (фильмы, места, кандидаты, прогоны, варианты) с атрибутами, к которому он вернётся; добавить колонку, поправить записи, сделать срез («покажи только…»). Таблица идёт в файл, а не текстом в ответ.\"\n"
"---\n"
"\n"
"# Данные проекта в берте\n"
"\n"
"Однотипные записи с атрибутами — список фильмов с рейтингами, места и\n"
"события, кандидаты, прогоны экспериментов — живут в проекте таблицами:\n"
"`.berth/data/<имя>.tsv`. Берт показывает их на странице проекта и умеет\n"
"делать срезы; файл читается глазами и разбирается `awk`.\n"
"\n"
"Это не задачи. Задачи (`.berth/tasks.md`) — про порядок работы и\n"
"состояние «сделано». Каталог — про записи с колонками, которые\n"
"сортируют и фильтруют. Не утрамбовывай записи в `tasks.md`.\n"
"\n"
"## Когда таблица нужна, а когда нет\n"
"\n"
"Это правило важнее остальных: не превращай в таблицу любой ответ.\n"
"\n"
"Таблица нужна, когда **все три** условия выполнены:\n"
"- записей три и больше, и они однотипные — у каждой те же атрибуты;\n"
"- у записей есть атрибуты, по которым их захотят сравнить или отобрать\n"
"  (год, рейтинг, цена, статус, дата);\n"
"- человек к списку вернётся: дополнит, отсортирует, спросит про срез.\n"
"\n"
"Одноразовый вопрос («какой из этих трёх лучше?») — просто ответ.\n"
"Список без атрибутов («что взять в поход») — обычный список в ответе или\n"
"в задаче. Сомневаешься — спроси одной строкой, вести ли таблицу.\n"
"\n"
"**Главное правило:** если человек просит собрать, дополнить, сравнить или\n"
"отсортировать такой список — результат идёт **в файл-таблицу**, а в\n"
"ответе только короткая сводка: сколько записей, что добавлено, что\n"
"бросилось в глаза. Таблицу в ответ не дублируй: она умрёт вместе с\n"
"сообщением, а файл останется.\n"
"\n"
"## Формат таблицы\n"
"\n"
"`.berth/data/<имя>.tsv` — TSV: колонки через табуляцию, первая строка —\n"
"заголовки. Имя файла — короткое, латиницей, по сути: `films.tsv`,\n"
"`places.tsv`, `runs.tsv`. Папку `.berth/data/` создай, если её нет.\n"
"\n"
"    название	год	тип	жанр	режиссёр	IMDb	проверено	статус\n"
"    Улика	1985	фильм	комедия	Джонатан Линн	7.3	2026-09-06	к просмотру\n"
"\n"
"- Не CSV: запятые внутри названий ломают разбор. Не Markdown-таблица:\n"
"  она разъезжается при каждой правке. Внутри значения не должно быть\n"
"  табуляций и переводов строки — замени пробелом.\n"
"- Заголовки — короткие, по-русски, в нижнем регистре (кроме имён\n"
"  собственных вроде IMDb). Первые колонки — те, по которым запись\n"
"  узнают (название, год), дальше атрибуты, в конце служебные (проверено,\n"
"  статус, заметка).\n"
"- Числа — числами без единиц и пробелов: `7.3`, а не `7,3` и не `7.3/10`;\n"
"  `1500`, а не `1 500 ₽`. Единицу вынеси в заголовок: `цена, ₽`. Иначе\n"
"  сортировка по колонке будет строковой.\n"
"- Даты — `ГГГГ-ММ-ДД`: сортируются как строки правильно.\n"
"- Неизвестное значение оставь **пустым**, а не выдумывай и не пиши «?».\n"
"- Значения, добытые из сети (рейтинги, цены, даты событий, наличие),\n"
"  сопровождай колонкой `проверено` с датой проверки: через полгода надо\n"
"  понимать, насколько цифра свежая. Обновил значение — обнови дату.\n"
"\n"
"## Правила правки\n"
"\n"
"- Добавил колонку — допиши её **во все строки**, хотя бы пустой.\n"
"- Порядок строк не меняй без просьбы: человек мог расставить их сам.\n"
"  Новые записи — в конец, если не сказано иначе.\n"
"- Не удаляй записи по своему усмотрению: помечай статусом\n"
"  (`отклонено`, `посмотрел`), удаляет человек.\n"
"- После правки проверь, что число табуляций одинаково во всех строках:\n"
"\n"
"      awk -F'\\t' 'NR==1{n=NF;next} NF!=n{print FILENAME\": строка \"NR\": \"NF\" колонок, надо \"n}' .berth/data/<имя>.tsv\n"
"\n"
"  Пустой вывод — файл цел.\n"
"\n"
"## Срезы: `.berth/views.tsv`\n"
"\n"
"Срез — команда-конвейер, чей вывод и есть таблица (тот же TSV с той же\n"
"строкой заголовков). Список срезов — `.berth/views.tsv`, по строке на\n"
"срез: имя, табуляция, команда. Строки с `#` в начале — комментарии.\n"
"\n"
"    # имя	команда (запускается из корня проекта через sh -c)\n"
"    Фильмы	awk -F'\\t' 'NR==1||$3==\"фильм\"' .berth/data/films.tsv\n"
"    От 7.5	awk -F'\\t' 'NR==1||$6>=7.5' .berth/data/films.tsv\n"
"    По рейтингу	awk 'NR==1' .berth/data/films.tsv; awk 'NR>1' .berth/data/films.tsv | sort -t$'\\t' -k6,6nr\n"
"\n"
"- Команда обязана **сохранять строку заголовков** — `NR==1||…` в `awk`,\n"
"  либо печатать её отдельно, как в примере с сортировкой.\n"
"- Команда запускается из корня проекта, путь к файлу — относительный.\n"
"  Внешние утилиты — только те, что есть везде: `awk`, `sort`, `grep`,\n"
"  `head`. Никаких сетевых запросов и записи файлов: срез читает, а не\n"
"  меняет.\n"
"- Срез должен отрабатывать за доли секунды: берт ждёт его не дольше двух\n"
"  секунд.\n"
"- Срезы заводятся **по просьбе** человека: «хочу видеть только короткие»,\n"
"  «покажи сериалы отдельно» — допиши строку в `views.tsv` и в ответе\n"
"  скажи, как она называется. Одноразовый вопрос про срез — ответь\n"
"  командой в терминале или сводкой, файл не трогай.\n"
"- Имя среза — короткое, по-русски, как подпись кнопки: «Сериалы»,\n"
"  «Дешевле 2000», «Свежие».\n"
"\n"
"## Что делать по просьбе\n"
"\n"
"- **Собрать список** — заведи `.berth/data/<имя>.tsv` с заголовками и\n"
"  записями; в ответе — сколько записей и какие колонки. Для значений из\n"
"  сети — колонка `проверено` с сегодняшней датой. Тут же добавь таблицу\n"
"  в реестр документов `.berth/files.tsv` (путь, табуляция, что это одной\n"
"  фразой — см. скилл `berth-files`): так она появится на странице проекта.\n"
"- **Дополнить** — допиши строки в конец, все колонки; новых колонок без\n"
"  нужды не заводи.\n"
"- **Сравнить или отсортировать** — не переписывай файл ради порядка:\n"
"  ответь сводкой, а если человек хочет видеть это постоянно — срезом в\n"
"  `views.tsv`.\n"
"- **Обновить рейтинги/цены** — перепиши значения и дату в `проверено`;\n"
"  в ответе перечисли, что изменилось.\n"
"- **Перенести список из tasks.md или из текста** — заведи таблицу, а в\n"
"  исходном месте оставь ссылку на файл; записи человека не удаляй без\n"
"  спроса.\n"
"\n"
"Проверь после правки: заголовок на месте, число колонок одинаково во всех\n"
"строках, число записей сходится с тем, что ты собирался записать.\n"
},
{ "berth-files",
"---\n"
"name: berth-files\n"
"description: \"Реестр документов проекта в берте — файл .berth/files.tsv: путь и пояснение, что это, одной фразой. Use when you create or substantially rewrite a document, table, report, draft or export meant for a human to read (not code), or when the user asks «опиши, что лежит в проекте», «что это за файл», «разбери документы», «обнови реестр файлов». Пояснение идёт в файл, а не в ответ.\"\n"
"---\n"
"\n"
"# Реестр документов проекта\n"
"\n"
"В папке проекта копятся документы, таблицы, отчёты и черновики, и через\n"
"неделю не понять, что есть что. Реестр `.berth/files.tsv` отвечает на\n"
"вопрос «что это за файл» без открытия файла; берт показывает его на\n"
"странице проекта. Файл читается глазами и правится руками.\n"
"\n"
"## Формат\n"
"\n"
"По строке на документ: путь относительно корня проекта, табуляция,\n"
"пояснение одной фразой. Строки с `#` в начале — комментарии.\n"
"\n"
"    # путь	что это\n"
"    docs/themes.md	Проект системы тем: тема — файл в формате Ghostty, роли интерфейса выводятся из палитры; отложен до обсуждения\n"
"    ROADMAP.md	Позиционирование продукта и этапы работы; первые этапы закрыты, дальше — план\n"
"    .berth/data/films.tsv	Список фильмов и сериалов к просмотру с рейтингами IMDb и статусом\n"
"\n"
"- Пояснение — до 200 знаков, по-русски, как ответ на «что это?»: что за\n"
"  документ, о чём, в каком состоянии (черновик, актуален, отложен).\n"
"  Не пересказ содержимого и не повтор имени файла.\n"
"- Даты в реестре **нет**: она врёт после правки руками. Берт берёт время\n"
"  правки с самого файла.\n"
"- Порядок строк — порядок в файле, берт его не меняет. Новые строки — в\n"
"  конец, если человек не расставил их сам.\n"
"- Папку `.berth/` создай, если её нет. Путь — с прямыми слэшами, без\n"
"  `./` впереди.\n"
"\n"
"## Что попадает в реестр — главное правило\n"
"\n"
"Только файлы, **сделанные для чтения человеком**, а не для сборки:\n"
"документы, таблицы, отчёты, черновики, экспорты, картинки, презентации.\n"
"\n"
"Не попадают:\n"
"- код, тесты, конфиги, сборочные скрипты, зависимости — их описывает\n"
"  структура репозитория и паспорт; реестр из каждого `.c` утонет;\n"
"- служебные файлы берта: `.berth/tasks.md`, `journal.md`, `summary.md`,\n"
"  `views.tsv`, `subprojects.tsv`, сам `files.tsv` — у них своя страница;\n"
"- паспорт `CLAUDE.md`, `README.md`, лицензии (`LICENSE`, `COPYING`,\n"
"  `OFL.txt`), сборочные `.txt` (`CMakeLists.txt`, `requirements.txt`);\n"
"- то, что лежит в скрытых каталогах (кроме `.berth/data/`), в\n"
"  `node_modules`, `build`, `dist`, `vendor`, `target`, `.git`;\n"
"- вложенные репозитории — папки со своим `.git` внутри проекта\n"
"  (референсные исходники, сабмодули): их документы не наши.\n"
"\n"
"Сомневаешься, документ это или код (например, `docs/api.md`,\n"
"сгенерированный из исходников; ноутбук с расчётом) — спроси себя, откроет\n"
"ли его человек, чтобы прочитать. Откроет — в реестр.\n"
"\n"
"## Два триггера\n"
"\n"
"1. **Ты создал или существенно переписал такой файл** — допиши или\n"
"   обнови его строку тут же, в том же ходе, не дожидаясь просьбы. Новый\n"
"   файл — новая строка; переписанный — обнови пояснение, если оно\n"
"   перестало быть верным. Переименовал — поправь путь. Удалил — убери\n"
"   строку.\n"
"\n"
"2. **Человек просит описать, что лежит в проекте** («опиши файлы»,\n"
"   «что это за документы», «обнови реестр») — обойди папку по правилам\n"
"   выше, для каждого документа, которого в реестре нет, прочитай начало\n"
"   (первые 30–50 строк; у таблицы — заголовки и пару строк; у картинки —\n"
"   имя и размер, посмотри её, если можешь) и напиши строку.\n"
"   Существующие пояснения **не переписывай** без просьбы, даже если\n"
"   написал бы иначе. Строки на файлы, которых больше нет, убери и\n"
"   скажи об этом в ответе.\n"
"\n"
"   Список кандидатов можно получить так (из корня проекта):\n"
"\n"
"       find . -mindepth 1 \\( -path '*/.*' -o -name node_modules -o -name build -o -name dist -o -name vendor -o -name target -o -exec test -d {}/.git \\; \\) -prune -o \\\n"
"         -type f \\( -name '*.md' -o -name '*.txt' -o -name '*.tsv' -o -name '*.csv' -o -name '*.pdf' -o -name '*.png' -o -name '*.jpg' -o -name '*.docx' -o -name '*.xlsx' -o -name '*.key' -o -name '*.pptx' \\) -print | sort\n"
"\n"
"   плюс `.berth/data/*.tsv`. Из списка убери то, что по правилам не\n"
"   документ (README, LICENSE, CLAUDE.md, CMakeLists.txt).\n"
"\n"
"## В ответе\n"
"\n"
"Коротко: сколько строк добавлено, сколько убрано, что показалось\n"
"черновиком или устаревшим. Сам реестр в ответ не копируй.\n"
"\n"
"Проверь после правки: в каждой строке ровно одна табуляция, каждый путь\n"
"существует, служебных файлов берта и кода в реестре нет.\n"
},
};

const int SKILLS_BUILTIN_COUNT = (int)(sizeof(SKILLS_BUILTIN) / sizeof(SKILLS_BUILTIN[0]));
