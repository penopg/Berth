// Мелочи, общие для всех модулей.
#ifndef BERTH_COMMON_H
#define BERTH_COMMON_H

// Прямоугольник в экранных координатах (логические пиксели raylib).
//
// Демо Ghostling рисовало терминал во всё окно и брало размеры через
// GetScreenWidth/Height прямо внутри отрисовки и обработки мыши. Нам нужно
// несколько сессий в одном окне рядом с боковой панелью, поэтому область
// терминала всегда передаётся явно.
typedef struct {
    int x, y, w, h;
} Rect;

#endif // BERTH_COMMON_H
